# need to test for equivalence between two instances of Node
# Python cannot do so via == automatically, need to change truth conditions of __eq__
# Need a function to recurse through getattribute:getattr() list